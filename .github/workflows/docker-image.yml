name: Docker Image CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps:
      working-directory: . 
    - name: Build the Docker image
      run: pwd && docker build -t poc-ci:latest .

