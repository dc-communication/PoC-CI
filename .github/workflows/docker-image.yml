name: Docker Image CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps: 
    - name: Build the Docker image
      working-directory: /home/olive/actions-runner/_work/PoC-CI
      run: pwd && ls && docker build -t poc-ci:latest .

