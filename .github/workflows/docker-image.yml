name: Docker Image CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps: 
    - name: Build the Docker image
      working-directory: .
      run: pwd && ls && docker build -t poc-ci:latest .

