name: Docker Image CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps: 
    - name: Check path
      run: ls -la
    - name: Build the Docker image
      run: docker build -t poc-ci:latest .

